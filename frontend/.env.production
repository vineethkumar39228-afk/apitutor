# Dynamic Backend API Base URL for Production Server / Nginx
# Replace with your actual live domain when deployed (e.g., https://api.yourdomain.com/api or /api)
VITE_API_URL=http://localhost:5000/api
